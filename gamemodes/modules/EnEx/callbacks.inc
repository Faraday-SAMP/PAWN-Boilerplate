/*!
 * EnEx/callbacks.inc
 *
 > Priority: 0
 */

forward OnPlayerAnimationChange( playerid, animation, previousanimation );
forward OnPlayerKeyPress( playerid, pressedkeys, keys);
forward OnPlayerEnterInterior( playerid, enex_id, connected_entry );